<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhixindu.apply.core.lenderInfoBO.dao.LenderContactMapper">
    <resultMap id="BaseResultMap" type="com.zhixindu.apply.facade.lender.bo.ContactBO">
        <id column="contact_id" jdbcType="INTEGER" property="contact_id"/>
        <result column="lender_id" jdbcType="INTEGER" property="lender_id"/>
        <result column="contact_relationship" jdbcType="INTEGER" property="contact_relationship" typeHandler="com.zhixindu.commons.handler.type.EnumValueTypeHandler"/>
        <result column="contact_name" jdbcType="VARCHAR" property="contact_name"/>
        <result column="contact_mobile" jdbcType="VARCHAR" property="contact_mobile"/>
    </resultMap>

    <sql id="Base_Column_List">
      contact_id, lender_id, contact_relationship, contact_name, contact_mobile
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from lender_contact
        where contact_id = #{contact_id,jdbcType=INTEGER}
    </select>

    <insert id="insert" parameterType="com.zhixindu.apply.facade.lender.bo.ContactBO">
        <selectKey keyProperty="contact_id" resultType="int" order="AFTER">
            select LAST_INSERT_ID()
        </selectKey>
        insert into lender_contact (lender_id, contact_relationship,
          contact_name, contact_mobile)
        values (#{lender_id,jdbcType=INTEGER}, #{contact_relationship,jdbcType=INTEGER},
          #{contact_name,jdbcType=VARCHAR}, #{contact_mobile,jdbcType=VARCHAR})
     </insert>

    <update id="updateByPrimaryKey" parameterType="com.zhixindu.apply.facade.lender.bo.ContactBO">
        update lender_contact
        set lender_id = #{lender_id,jdbcType=INTEGER},
          contact_relationship = #{contact_relationship,jdbcType=INTEGER},
          contact_name = #{contact_name,jdbcType=VARCHAR},
          contact_mobile = #{contact_mobile,jdbcType=VARCHAR}
        where contact_id = #{contact_id,jdbcType=INTEGER}
    </update>

    <select id="selectByLenderId" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from lender_contact
        where lender_id = #{lender_id,jdbcType=INTEGER}
    </select>

</mapper>